# Build-time dependencies for packaging CHIRP.app on macOS.
#
# wxPython==4.2.0 matches the pin in the repo's top-level requirements.txt
# for platform_system != "Linux" -- this is the same wxPython build the
# application is meant to run against, not a packaging-only choice. PyPI
# only publishes this as a macosx_*_universal2 wheel, and only for CPython
# 3.10 (not 3.11/3.12) -- which is why the macOS build pins Python 3.10,
# matching the AppImage's python3.10 as well. No source build or GTK-style
# native stack is needed the way the Linux AppImage requires.
wxPython==4.2.0
pyserial
requests
yattag
suds
lark
pyinstaller>=6.10,<7
